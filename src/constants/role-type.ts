export enum RoleType {
  USER = 'USER',
  ADMIN = 'ADMIN',
}
export enum RateType {
  mund = 'mund',
  kg = 'kg',
}
export enum FixType {
  factory = 'factory',
  farm = 'farm',
}
export enum SalType {
  add = 1,
  deduct =0,
}
export enum Type {
  downloader = 'Downloader',
  uploader = 'Uploader',
  carriager = 'Carriager',
}
